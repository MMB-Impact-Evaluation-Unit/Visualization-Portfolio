---
title: "Histogram plot with new colors (accessible)"
author: "Katie Kotasek"
date: "2026-02-05"
editor_options: 
  chunk_output_type: console
---

```{r packages}
# default packages
library(mmbtools)
library(tidyverse)

# accessibility package(s)
library(colorBlindness)

mmb_load_fonts(quiet=TRUE)
```

```{r}
# generate data
set.seed(123)
data <- data.frame(
    value = c(rnorm(50, mean = 60, sd = 10), rnorm(50, mean = 70, sd = 15))
    ,Group = rep(c("A","B","C","D","E","F"), each = 50)
)
```

```{r plot}
data %>%
    ggplot(aes(x=value,fill=Group)) +
    geom_histogram(
        position="dodge"
        ,bins = 10
        ,width=0.7
        ,color="black"    # adds black border around bars
    ) +
    scale_x_continuous(n.breaks=6) +
    scale_y_continuous(expand = c(0, 0),n.breaks=8) +
    scale_fill_manual(values=c(
        "A"="#003865","B"="#78BE21","C"="#008EAA","D"="#A4BCC2","E"="#404788","F"="#FDEC51")
    ) +
    labs(x = "Value",y = "Frequency") +
    theme_mmb_accessible(legend.position="bottom")
```

```{r}
# save to PNG
ggsave(
    filename = "Histogram plot with new colors (accessible).png",
    width=7, # Maximum plot width should be 6.5 or 7
    height=7 * 10/16, # Scale this however you want. Current is 16:10
    units="in"
)
```
